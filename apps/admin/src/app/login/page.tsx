import { LoginPageClient } from '@/components/login-page-client';

/**
 * Renders the login page for the local dummy admin flow.
 */
export default function LoginPage() {
  return <LoginPageClient />;
}
