import { StoryEditorPageClient } from '@/components/story-editor-page-client';

/**
 * Renders the new Story creation page.
 */
export default function NewStoryPage() {
  return <StoryEditorPageClient />;
}
